Office Management System

This project is a simple office management system built using Laravel and MySQL. It allows managing companies and employees, along with some additional features like filtering, pagination, and API-based location selection.

The main goal of this project was to implement CRUD operations and integrate different concepts like relationships, DataTables, and external APIs.